# Backend Environment Variables for Production
# Copy this file to .env and fill in your values

# JWT Secret Key (IMPORTANT: Generate a secure random string, min 32 characters)
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars

# JWT Algorithm
ALGORITHM=HS256

# Access token expiration time in minutes (10080 = 7 days)
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# CORS Origins (comma-separated list for production)
# Note: Backend automatically accepts all *.pages.dev domains via regex
# Example: https://payment-transactions.pages.dev,https://your-custom-domain.com
CORS_ORIGINS=http://localhost:5173,https://payment-transactions.pages.dev
