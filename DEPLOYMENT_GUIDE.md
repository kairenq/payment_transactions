# 🚀 Гайд по развертыванию Payment Transactions System

Полное руководство по развертыванию приложения на **Render** (backend) и **Netlify** (frontend).

---

## 📋 Содержание

1. [Подготовка](#подготовка)
2. [Развертывание Backend на Render](#развертывание-backend-на-render)
3. [Развертывание Frontend на Netlify](#развертывание-frontend-на-netlify)
4. [Финальная настройка](#финальная-настройка)
5. [Тестирование](#тестирование)
6. [Troubleshooting](#troubleshooting)

---

## 🎯 Подготовка

### 1. Проверьте что все файлы на месте

Убедитесь что у вас есть эти файлы:

```
payment_transactions/
├── render.yaml                    # ✅ Конфигурация Render
├── backend/
│   ├── start.sh                   # ✅ Скрипт запуска
│   ├── requirements.txt           # ✅ Python зависимости
│   └── .env.example              # ✅ Пример env переменных
└── frontend/
    ├── netlify.toml               # ✅ Конфигурация Netlify
    └── .env.example              # ✅ Пример env переменных
```

### 2. Запушьте код на GitHub

```bash
git add .
git commit -m "Add deployment configuration for Render and Netlify"
git push origin main
```

**ВАЖНО:** Убедитесь что вы пушите в ветку `main` или укажите правильную ветку!

---

## 🔧 Развертывание Backend на Render

### Шаг 1: Создайте аккаунт на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите **Sign Up**
3. Зарегистрируйтесь через **GitHub** (рекомендуется)
4. Авторизуйте Render для доступа к вашим репозиториям

### Шаг 2: Создайте новый Web Service

1. На главной странице Render нажмите **New +**
2. Выберите **Web Service**
3. Подключите ваш GitHub репозиторий:
   - Нажмите **Connect** рядом с репозиторием `payment_transactions`
   - Если не видите репозиторий, нажмите **Configure account** и дайте доступ

### Шаг 3: Настройте Web Service

Заполните форму:

#### Basic Settings:
- **Name:** `payment-transactions-api` (или любое имя)
- **Region:** `Oregon (US West)` (или ближайший к вам)
- **Branch:** `main` (или ваша основная ветка)
- **Root Directory:** `backend`

#### Build & Deploy:
- **Runtime:** `Python 3`
- **Build Command:**
  ```bash
  pip install -r requirements.txt
  ```
- **Start Command:**
  ```bash
  sh start.sh
  ```

#### Instance Type:
- **Plan:** `Free` (для начала)

### Шаг 4: Настройте Environment Variables

Прокрутите вниз до секции **Environment Variables** и добавьте:

1. **SECRET_KEY**
   - Нажмите **Generate** для автоматической генерации
   - Или создайте свой:
   ```bash
   python -c "import secrets; print(secrets.token_urlsafe(32))"
   ```

2. **ALGORITHM**
   - Value: `HS256`

3. **ACCESS_TOKEN_EXPIRE_MINUTES**
   - Value: `10080`

4. **CORS_ORIGINS** (ПОКА ОСТАВЬТЕ ПУСТЫМ, вернемся к этому позже)

### Шаг 5: Деплой!

1. Нажмите **Create Web Service**
2. Render начнет деплой:
   - ⏳ Building... (2-3 минуты)
   - ⏳ Deploying...
   - ✅ **Live!**

3. Скопируйте URL вашего API:
   ```
   https://payment-transactions-api-xxxx.onrender.com
   ```

   **СОХРАНИТЕ ЭТОТ URL!** Он понадобится для frontend!

### Шаг 6: Проверьте что backend работает

Откройте в браузере:
```
https://your-app-name.onrender.com
```

Вы должны увидеть:
```json
{
  "message": "Payment Transactions System API",
  "version": "1.0.0",
  "docs": "/docs",
  "status": "running"
}
```

✅ **Backend развернут!**

---

## 🎨 Развертывание Frontend на Netlify

### Шаг 1: Создайте аккаунт на Netlify

1. Перейдите на [netlify.com](https://netlify.com)
2. Нажмите **Sign Up**
3. Зарегистрируйтесь через **GitHub**
4. Авторизуйте Netlify для доступа к репозиториям

### Шаг 2: Создайте новый сайт

1. На главной странице нажмите **Add new site** → **Import an existing project**
2. Выберите **GitHub**
3. Найдите и выберите репозиторий `payment_transactions`

### Шаг 3: Настройте Build Settings

#### Basic Settings:
- **Branch to deploy:** `main`
- **Base directory:** `frontend`
- **Build command:** `npm run build`
- **Publish directory:** `frontend/dist`

#### Advanced Settings (нажмите "Show advanced"):

Добавьте Environment Variable:

- **Key:** `VITE_API_BASE_URL`
- **Value:** `https://your-render-app.onrender.com/api`

  ⚠️ **ВАЖНО:** Замените `your-render-app` на ваш НАСТОЯЩИЙ URL Render (из Шага 5 выше)!

  Пример:
  ```
  https://payment-transactions-api-xxxx.onrender.com/api
  ```

  **НЕ ЗАБУДЬТЕ `/api` В КОНЦЕ!**

### Шаг 4: Деплой!

1. Нажмите **Deploy site**
2. Netlify начнет деплой:
   - ⏳ Building...
   - ⏳ Publishing...
   - ✅ **Published!**

3. Netlify присвоит случайный URL:
   ```
   https://random-name-12345.netlify.app
   ```

### Шаг 5: (Опционально) Настройте кастомное имя

1. Перейдите в **Site settings** → **Domain management**
2. Нажмите **Options** → **Edit site name**
3. Введите желаемое имя:
   ```
   payment-transactions
   ```
4. Ваш новый URL:
   ```
   https://payment-transactions.netlify.app
   ```

**СКОПИРУЙТЕ ЭТОТ URL!** Он понадобится для финальной настройки CORS!

---

## 🔗 Финальная настройка

### Обновите CORS на Render

Теперь когда у вас есть Netlify URL, нужно разрешить CORS:

1. Вернитесь на [Render Dashboard](https://dashboard.render.com)
2. Откройте ваш Web Service
3. Перейдите в **Environment**
4. Найдите или добавьте переменную **CORS_ORIGINS**
5. Установите значение (замените на ваш реальный URL):
   ```
   https://payment-transactions.netlify.app
   ```

   Если нужно несколько URL (например, для production и staging):
   ```
   https://payment-transactions.netlify.app,https://staging.netlify.app
   ```

6. Нажмите **Save Changes**
7. Render автоматически передеплоит приложение (1-2 минуты)

✅ **Готово! Приложение полностью развернуто!**

---

## ✅ Тестирование

### 1. Откройте ваш сайт

Перейдите на ваш Netlify URL:
```
https://payment-transactions.netlify.app
```

### 2. Проверьте в консоли браузера (F12)

Вы должны увидеть:
```
🌐 API Base URL: https://payment-transactions-api-xxxx.onrender.com/api
```

### 3. Попробуйте войти

- **Username:** `admin`
- **Password:** `admin123`

### 4. Проверьте что всё работает

- ✅ Вход успешен
- ✅ Dashboard загружается
- ✅ Транзакции работают
- ✅ Аналитика показывается
- ✅ Админ панель доступна

### 5. Проверьте в консоли

Вы должны видеть:
```
✅ User authenticated: admin
200 OK на все запросы
```

**НЕТ ошибок 401, 403, CORS!**

---

## 🛠 Troubleshooting

### Проблема: CORS Error в браузере

**Ошибка:**
```
Access to XMLHttpRequest has been blocked by CORS policy
```

**Решение:**
1. Проверьте что CORS_ORIGINS на Render включает ваш Netlify URL
2. URL должен быть ТОЧНО такой же (проверьте https vs http)
3. НЕ добавляйте `/` в конце URL при настройке CORS

### Проблема: 404 на страницах при обновлении

**Ошибка:**
```
Page Not Found
```

**Решение:**
Проверьте что `netlify.toml` правильно настроен с redirects:
```toml
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### Проблема: Environment variables не работают

**Симптом:**
API URL показывает `localhost` в production

**Решение:**
1. В Netlify: **Site settings** → **Environment variables**
2. Проверьте что `VITE_API_BASE_URL` установлен
3. **ВАЖНО:** Переменные Vite должны начинаться с `VITE_`
4. После изменения нажмите **Trigger deploy** → **Clear cache and deploy**

### Проблема: Render Web Service не запускается

**Ошибка в логах:**
```
No module named 'xxx'
```

**Решение:**
1. Проверьте `backend/requirements.txt`
2. Убедитесь что все зависимости указаны
3. Проверьте Build Command: `pip install -r requirements.txt`

### Проблема: Database пустая каждый раз

**Причина:**
Render Free tier не сохраняет файлы между деплоями

**Решение для production:**
- Используйте Render PostgreSQL Database (бесплатный tier доступен)
- Или используйте другой внешний database сервис

---

## 🎉 Поздравляем!

Ваше приложение **Payment Transactions System** теперь доступно онлайн!

### Ссылки:

- **Frontend:** https://your-app.netlify.app
- **Backend API:** https://your-app.onrender.com
- **API Docs:** https://your-app.onrender.com/docs

### Следующие шаги:

1. 🎨 Настройте кастомный домен
2. 🔒 Добавьте SSL (автоматически на Netlify)
3. 📊 Настройте мониторинг (Render автоматически)
4. 🗄️ Перейдите на PostgreSQL для persistence
5. 📧 Добавьте email уведомления

---

## 📚 Дополнительные ресурсы

- [Render Documentation](https://render.com/docs)
- [Netlify Documentation](https://docs.netlify.com)
- [FastAPI Deployment](https://fastapi.tiangolo.com/deployment/)
- [Vite Environment Variables](https://vitejs.dev/guide/env-and-mode.html)

---

**Нужна помощь?** Создайте issue в GitHub репозитории!
