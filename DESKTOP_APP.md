# 🖥️ Desktop Application Guide

Превращаем веб-приложение в **полноценное десктопное приложение** без браузера!

---

## 🎯 Что это даёт?

✅ **Отдельное окно приложения** (не браузер)
✅ **Иконка на рабочем столе**
✅ **Работает как обычная программа**
✅ **.exe инсталлятор** для Windows
✅ **Нет адресной строки браузера**
✅ **Работает offline** (после установки)

---

## 📦 Технология: Electron

**Electron** = тот же Chrome, но выглядит как нативное приложение.

Известные приложения на Electron:
- Visual Studio Code
- Discord
- Slack
- Spotify
- Telegram Desktop

---

## 🚀 Быстрый старт (без сборки .exe)

### Шаг 1: Установи зависимости

Убедись что установлены:
- **Node.js 18+**: https://nodejs.org/
- **Python 3.8+**: https://www.python.org/

### Шаг 2: Запусти приложение

**Windows:**
```bash
start-desktop-app.bat
```

Двойной клик на файл → приложение откроется в отдельном окне!

**Linux/Mac:**
```bash
chmod +x start-desktop-app.sh
./start-desktop-app.sh
```

---

## 📦 Сборка .exe инсталлятора (для Windows)

Если хочешь создать `.exe` файл для установки на других компьютерах:

### Шаг 1: Запусти сборку

```bash
build-desktop-app.bat
```

Процесс займёт **5-10 минут**.

### Шаг 2: Найди .exe файл

После сборки инсталлятор будет здесь:
```
electron\dist\Payment Transactions Setup 1.0.0.exe
```

### Шаг 3: Установи

Двойной клик на `.exe` → установка → ярлык на рабочем столе → готово!

---

## 🛠️ Ручная сборка (для продвинутых)

### 1. Собери frontend

```bash
cd frontend
npm install
npm run build
cd ..
```

### 2. Установи Electron

```bash
cd electron
npm install
```

### 3. Запусти (без сборки)

```bash
npm start
```

### 4. Собери .exe

```bash
npm run build:win
```

Или для других платформ:
```bash
npm run build:mac    # macOS (.dmg)
npm run build:linux  # Linux (.AppImage, .deb)
```

---

## 📁 Структура проекта

```
payment_transactions/
├── electron/
│   ├── main.js              # Главный файл Electron
│   ├── package.json         # Конфигурация сборки
│   ├── icon.png             # Иконка приложения
│   └── dist/                # Собранные .exe файлы (после build)
│
├── backend/                 # FastAPI сервер
├── frontend/dist/           # React приложение (собранное)
│
├── start-desktop-app.bat    # Быстрый запуск (dev mode)
└── build-desktop-app.bat    # Сборка .exe инсталлятора
```

---

## ⚙️ Как это работает?

```
┌────────────────────────────────────────┐
│  Electron Window (выглядит как app)    │
│                                        │
│  ┌──────────────────────────────────┐ │
│  │  Chromium (встроенный браузер)   │ │
│  │                                  │ │
│  │  ┌────────────────────────────┐ │ │
│  │  │   React UI                 │ │ │
│  │  │   (localhost:8000)         │ │ │
│  │  └────────────────────────────┘ │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────┐
│  FastAPI Server (Python)               │
│  - Запускается автоматически           │
│  - Работает в фоне                     │
│  - Останавливается при закрытии окна   │
└────────────────────────────────────────┘
```

**Что происходит:**
1. Двойной клик → Electron запускается
2. Electron запускает Python backend (FastAPI)
3. Electron открывает окно и загружает `http://127.0.0.1:8000`
4. Пользователь видит приложение (не видит адресную строку)
5. При закрытии окна → Electron останавливает backend

---

## 🎨 Кастомизация

### Изменить иконку

1. Создай иконку `icon.png` (256x256 или 512x512)
2. Положи в папку `electron/`
3. Пересобери: `npm run build:win`

### Изменить название окна

Открой `electron/main.js`:
```javascript
title: 'Твоё название приложения'
```

### Убрать DevTools (консоль разработчика)

Открой `electron/main.js`, найди:
```javascript
devTools: true  // Измени на false
```

### Изменить размер окна

```javascript
width: 1400,  // Ширина
height: 900,  // Высота
```

---

## 📦 Размер приложения

После сборки `.exe` инсталлятор будет весить:
- **~150-200 MB** (включает Chromium + Python + всё приложение)

После установки займёт:
- **~300-400 MB** на диске

Это нормально для Electron приложений.

---

## 🐛 Проблемы и решения

### Приложение не запускается

1. **Проверь Python**:
   ```bash
   python --version
   ```
   Должно быть 3.8+

2. **Проверь Node.js**:
   ```bash
   node --version
   ```
   Должно быть 18+

3. **Проверь зависимости backend**:
   ```bash
   cd backend
   pip install -r requirements.txt
   ```

### Окно открывается пустое

Подожди 3-5 секунд - backend стартует.

Если не помогло:
1. Закрой приложение
2. Удали `backend/database.db`
3. Запусти снова

### Ошибка при сборке .exe

**Ошибка**: `electron-builder not found`

**Решение**:
```bash
cd electron
npm install electron-builder --save-dev
```

---

## 🚀 Распространение приложения

### Вариант 1: Отдай .exe инсталлятор

1. Собери: `build-desktop-app.bat`
2. Найди: `electron\dist\Payment Transactions Setup 1.0.0.exe`
3. Отправь файл пользователю
4. Пользователь двойной клик → установка → готово!

**Плюсы**:
- ✅ Простая установка
- ✅ Ярлык на рабочем столе
- ✅ Деинсталлятор в Programs & Features

### Вариант 2: Portable версия (без установки)

```bash
cd electron
npm run pack
```

Результат: `electron\dist\win-unpacked\` - папка с приложением.

Скопируй всю папку → запусти `Payment Transactions.exe` → работает!

---

## 🔐 Безопасность

⚠️ **Важно**:
- `.exe` файл содержит весь исходный код
- Не храни пароли и ключи в коде
- Используй переменные окружения

Для production:
1. Убери DevTools в `main.js`
2. Удали console.log
3. Зашифруй `SECRET_KEY`

---

## 📊 Альтернативы Electron

Если нужно легковесное приложение:

### Tauri (Rust)
- **Размер**: ~10 MB (вместо 200 MB)
- **Скорость**: Быстрее
- **Сложность**: Выше (нужен Rust)

### PyWebView (Python)
- **Размер**: ~50 MB
- **Простота**: Очень просто
- **Минусы**: Меньше возможностей

### CEF Python
- **Размер**: ~100 MB
- **Python-native**: Да
- **Минусы**: Меньше документации

---

## 📝 Итог

Теперь у тебя есть **полноценное десктопное приложение**!

**Для разработки**:
```bash
start-desktop-app.bat  # Быстрый старт
```

**Для распространения**:
```bash
build-desktop-app.bat  # Сборка .exe
```

Всё! 🎉
